{
  "name": "hub-hibrid-yo8acr-pro",
  "version": "2.3.0",
  "type": "module",
  "description": "YO8ACR Weather Hub PRO — ANM FIRST hybrid weather intelligence platform",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "verify": "npm run lint && node scripts/check-inline.js && npm test",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.3.3",
    "vitest": "^2.0.0"
  }
}
